# 저장소 정리 기록 — 2026-09-22

기준 커밋: `c7e1499`. 작업 브랜치: `chore/repository-refresh-20260922`.

## 추적 정리

| 대상 | 조치 | 근거 |
| --- | --- | --- |
| `ChatBot/node_modules/` 563개 파일 | Git 추적 해제; 로컬 보존 | 설치 산출물이며 부분 설치 상태. 테스트는 frontend의 잠금 파일/의존성을 재사용 |
| `outputs/`의 JSON·CSV 5개 | Git 추적 해제; 로컬 보존 | 관리 명령의 재생성 결과, 실행 입력 참조 없음 |
| `frontend/vercelupdatetool.py` | 삭제 | 내용은 숫자 `1` 한 글자이며 호출 참조 없음 |
| 제출용 Word·Excel 5개 | `docs/archive/submissions/` 이동 | 과거 제출 원본 보존, 현재 운영 설명과 구분 |
| 루트 `.gitignore` | 설치 폴더·빌드·outputs 제외 | 다른 하위 폴더에서 재발 방지 |
| `.dockerignore` | 환경 비밀값·캐시·설치 결과·문서 제외 | 이미지 빌드에 불필요한 로컬 상태 유입 축소 |

이 조치는 다음 커밋의 파일 구성을 정리합니다. 과거 Git 이력을 다시 쓰거나 저장소 히스토리 용량을 제거하지 않았습니다.

## 보존한 것

모델 가중치·토크나이저 자산·테스트 fixture·seed 입력·참조 이미지·소개 페이지 영상/오디오는 배포 또는 재현 경로가 있을 수 있어 유지했습니다. `landing/`과 `showcase/`는 독립 소개 경험입니다. SQLite 어댑터와 번들 도구는 명시적 오프라인/이전 환경 경로가 있어 자동 삭제하지 않았습니다.

## 문서와 테스트 실행기

메인 README를 브랜드 표지·기능 요약·Mermaid 구성도·Shields 배지·문서 탐색 구조로 교체했습니다. 현재 구조·개발·배포·데이터·버전·새 채팅용 컨텍스트 문서를 분리했습니다. 하위 README와 작업 규칙의 옛 외부 저장소 경로를 현재 모노레포 구조로 갱신했습니다.

과거 인수인계와 설계 기록에는 시점 표시를 추가하고, 이전 메인 README 원문도 보존했습니다. 개발 로그는 기존 본문을 수정하지 않고 이번 기록만 추가했습니다. 의존성 버전·서비스 동작·운영 데이터는 변경하지 않았습니다.

챗봇 브라우저 테스트 실행기는 현재 `frontend/`를 기본 경로로 사용하고, 상대 import 구조를 보존하며, 고유한 임시 디렉터리를 종료 시 정리하도록 수정했습니다. 별도 ChatBot node_modules를 요구하지 않습니다.

## 검증 결과

실행 환경: 로컬 Node 24.18.0. 프로젝트 `.nvmrc`의 Node 20 계열 검증은 별도로 수행하지 않았습니다.

| 검사 | 결과 |
| --- | --- |
| `frontend/npm run build` | 통과. 기존 정적/동적 import 혼용 경고 2종 |
| `frontend/npm test` | API 29건 + 할인 필터 7건 통과 후 중단. 정리 전과 동일하게 `MutationObserver is not defined` |
| `bash ChatBot/tests/run.sh` | 7개 파일 중 3개 통과. 4개는 `document` 또는 소개 화면 DOM 누락으로 실패 |
| 챗봇 `test_fit_handoff.py` | 17건 통과 |
| 챗봇 `test_mdclean.py` | 3건 통과 |
| README 표지 | SVG를 PNG로 렌더링하여 배치·잘림 확인 |
| 현재 문서 상대 링크 | 대상 파일 존재 확인 |
| Git 공백 검사 | 작업 트리·인덱스 모두 통과 |

챗봇 테스트는 정리 전 7개 모두 jsdom/모듈 경로 문제로 실패했습니다. 실행기 수정 후 드러난 화면 초기화 실패를 감추기 위해 테스트를 제외하거나 서비스 코드를 바꾸지 않았습니다. 전체 테스트 통과나 운영 배포 성공으로 해석하면 안 됩니다.

운영 서버·DB 마이그레이션·실제 모델 API·Docker 이미지 빌드는 이번 정리에서 실행하지 않았습니다. 커밋·푸시·배포도 수행하지 않았습니다.
