# 버셀에 올릴 필요 없는 것.
#
# ★ Root Directory 를 frontend 로 잡으면 버셀은 이 폴더만 올린다.
#   그래도 아래 것들은 빌드에도 배포에도 안 쓰이므로 빼 둔다.
#   (dist 는 버셀이 직접 만든다. node_modules 는 npm install 로 다시 깐다.)

_to_delete/
tests/
docs/
AGENTS.md
CLAUDE_프로젝트_지침.md

.DS_Store
**/.DS_Store

node_modules/
dist/
